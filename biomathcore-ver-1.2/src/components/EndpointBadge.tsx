"use client";

// No-op EndpointBadge: renders nothing.
// We keep the file/exports so imports don't break, but nothing will be shown.
export default function EndpointBadge(_props: { path: string; className?: string }) {
  return null;
}
